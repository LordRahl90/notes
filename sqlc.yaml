version: "2"
servers: [ ]
sql:
  - engine: mysql
    queries: "queries"
    schema: "migrations"
    gen:
      go:
        package: "repositories"
        out: "repositories"
overrides:
  go: null
plugins: [ ]
rules: [ ]
options: { }
